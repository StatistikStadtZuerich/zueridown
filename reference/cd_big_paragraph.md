# Lead paragraph

Generates 'asis' LaTeX code for a large, emphasised lead paragraph (for
example a summary at the start of a report).

## Usage

``` r
cd_big_paragraph(text = default(""))
```

## Arguments

- text:

  Character. The paragraph text.
